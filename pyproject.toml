[project]
name = "my-fts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.0",
    "fugashi[unidic]>=1.5.2",
    "msgpack>=1.1.2",
]

[tool.ty.environment]
extra-paths = ["./typings"]

[tool.vercel.scripts]
build = "uv run build_index.py"
