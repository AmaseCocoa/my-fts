[project]
name = "my-fts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fugashi[unidic]>=1.5.2",
]

[tool.ty.environment]
extra-paths = ["./typings"]
